// AUTO-GENERATED by scripts/build-runtime.mjs (source: src/runtime/**) — DO NOT EDIT.
let forwardingEnabled = true;
function setLogForwarding(on) {
  forwardingEnabled = !!on;
}
function postLog(level, text) {
  var _a;
  if (!forwardingEnabled) return;
  try {
    (_a = window.parent) == null ? void 0 : _a.postMessage(
      { __editorPreviewLog: true, level, text: String(text), time: (/* @__PURE__ */ new Date()).toLocaleTimeString() },
      "*"
    );
  } catch {
  }
}
function fail(msg) {
  const text = String(msg);
  const errorEl = document.getElementById("error");
  if (errorEl) {
    errorEl.textContent = "预览运行失败\n\n" + text;
    errorEl.classList.add("visible");
  }
  postLog("error", text);
  console.error(text);
}
export {
  fail,
  postLog,
  setLogForwarding
};
