// AUTO-GENERATED by scripts/build-runtime.mjs (source: src/runtime/**) — DO NOT EDIT.
import { state, numOr, registry } from "./state.mjs";
import { getEntity, deepFind } from "./entity.mjs";
import { resolveNodeEntity, resolveScriptInstance } from "./runtime.mjs";
import { builtinTypeKeyOf, builtinFacadeOf } from "./component-registry.mjs";
const animationApi = {
  play(entity, clip) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.play(entity == null ? void 0 : entity.id, clip);
  },
  stop(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.stop(entity == null ? void 0 : entity.id);
  },
  pause(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.pause(entity == null ? void 0 : entity.id);
  },
  resume(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.resume(entity == null ? void 0 : entity.id);
  }
};
const audioApi = {
  play(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.play(entity == null ? void 0 : entity.id);
  },
  stop(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.stop(entity == null ? void 0 : entity.id);
  },
  pause(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.pause(entity == null ? void 0 : entity.id);
  },
  resume(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.resume(entity == null ? void 0 : entity.id);
  },
  setVolume(entity, volume) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.setVolume(entity == null ? void 0 : entity.id, volume);
  }
};
const particlesApi = {
  play(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.play(entity == null ? void 0 : entity.id);
  },
  pause(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.pause(entity == null ? void 0 : entity.id);
  },
  stop(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.stop(entity == null ? void 0 : entity.id);
  },
  restart(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.restart(entity == null ? void 0 : entity.id);
  },
  clear(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.clear(entity == null ? void 0 : entity.id);
  },
  stateOf(entity) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.infoOf(entity == null ? void 0 : entity.id)) ?? null;
  },
  setSettings(entity, patch) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.updateSettings(entity == null ? void 0 : entity.id, patch);
  }
};
const physicsApi = {
  applyImpulse(entity, x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.applyImpulse(entity == null ? void 0 : entity.id, numOr(x, 0), numOr(y, 0), numOr(z, 0));
  },
  applyForce(entity, x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.applyForce(entity == null ? void 0 : entity.id, numOr(x, 0), numOr(y, 0), numOr(z, 0));
  },
  setLinearVelocity(entity, x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.setLinearVelocity(entity == null ? void 0 : entity.id, numOr(x, 0), numOr(y, 0), numOr(z, 0));
  },
  setAngularVelocity(entity, x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.setAngularVelocity(entity == null ? void 0 : entity.id, numOr(x, 0), numOr(y, 0), numOr(z, 0));
  },
  getLinearVelocity(entity) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.getLinearVelocity(entity == null ? void 0 : entity.id)) ?? null;
  },
  bodyInfo(entity) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.bodyInfo(entity == null ? void 0 : entity.id)) ?? null;
  },
  setGravityScale(entity, scale) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.setGravityScale(entity == null ? void 0 : entity.id, numOr(scale, 1));
  },
  wakeUp(entity) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.wakeUp(entity == null ? void 0 : entity.id);
  },
  setGravity(x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.setGravity(numOr(x, 0), numOr(y, -9.81), numOr(z, 0));
  },
  /** 射线投射（世界级查询，不按实体寻址；Worker 模式返回 Promise） */
  castRay(options) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.castRay(options)) ?? [];
  }
};
function findOnEntity(entity, token) {
  const typeKey = builtinTypeKeyOf(token);
  if (typeKey) return builtinFacadeOf(entity, typeKey);
  if (typeof token === "function") {
    const list = state.componentsByNode.get(entity.id);
    return list ? list.find((c) => c instanceof token) ?? null : null;
  }
  if (typeof token === "string") return resolveScriptInstance(entity.id, token);
  return null;
}
const sceneApi = {
  get root() {
    const rootObj = state.host && state.host.rootObj;
    return rootObj ? getEntity(rootObj) : null;
  },
  find(nameOrPath) {
    const rootObj = state.host && state.host.rootObj;
    if (!rootObj) return null;
    if (rootObj.name === nameOrPath) return getEntity(rootObj);
    const hit = deepFind(rootObj, nameOrPath);
    if (hit) return getEntity(hit);
    return resolveNodeEntity(nameOrPath);
  },
  findAll() {
    return registry().map((e) => getEntity(e.obj)).filter(Boolean);
  },
  findByTag(tag) {
    var _a, _b;
    for (const e of registry()) {
      if (((_b = (_a = e.obj) == null ? void 0 : _a.userData) == null ? void 0 : _b.nodeTag) === tag) return getEntity(e.obj);
    }
    return null;
  },
  findAllByTag(tag) {
    return registry().filter((e) => {
      var _a, _b;
      return ((_b = (_a = e.obj) == null ? void 0 : _a.userData) == null ? void 0 : _b.nodeTag) === tag;
    }).map((e) => getEntity(e.obj)).filter(Boolean);
  },
  findComponent(token) {
    for (const e of registry()) {
      const ent = getEntity(e.obj);
      if (!ent) continue;
      const hit = findOnEntity(ent, token);
      if (hit) return hit;
    }
    return null;
  },
  findComponents(token) {
    const out = [];
    for (const e of registry()) {
      const ent = getEntity(e.obj);
      if (!ent) continue;
      const hit = findOnEntity(ent, token);
      if (hit) out.push(hit);
    }
    return out;
  }
};
export {
  animationApi,
  audioApi,
  particlesApi,
  physicsApi,
  sceneApi
};
