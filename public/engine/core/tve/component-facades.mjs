// AUTO-GENERATED by scripts/build-runtime.mjs (source: src/runtime/**) — DO NOT EDIT.
import { state, numOr } from "./state.mjs";
import { BuiltinComponent, LOOP_MODES, CONDITION_OPS } from "./component-base.mjs";
class RigidBody extends BuiltinComponent {
  get mode() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.bodyInfo(this.entity.id)) == null ? void 0 : _c.mode) ?? "dynamic";
  }
  get gravityScale() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.bodyInfo(this.entity.id)) == null ? void 0 : _c.gravityScale) ?? 1;
  }
  get colliderCount() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.bodyInfo(this.entity.id)) == null ? void 0 : _c.colliderCount) ?? 0;
  }
  setGravityScale(s) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.setGravityScale(this.entity.id, numOr(s, 1));
  }
  setLinearVelocity(x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.setLinearVelocity(this.entity.id, numOr(x, 0), numOr(y, 0), numOr(z, 0));
  }
  getLinearVelocity() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.getLinearVelocity(this.entity.id)) ?? null;
  }
  applyImpulse(x, y, z) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.applyImpulse(this.entity.id, numOr(x, 0), numOr(y, 0), numOr(z, 0));
  }
  wakeUp() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.wakeUp(this.entity.id);
  }
}
class Collider extends BuiltinComponent {
  get shape() {
    var _a, _b;
    return ((_b = (_a = this.__json) == null ? void 0 : _a.collider) == null ? void 0 : _b.shape) ?? "box";
  }
  get isSensor() {
    var _a, _b;
    return ((_b = (_a = this.__json) == null ? void 0 : _a.collider) == null ? void 0 : _b.isSensor) === true;
  }
  get friction() {
    var _a, _b;
    return numOr((_b = (_a = this.__json) == null ? void 0 : _a.collider) == null ? void 0 : _b.friction, 0.6);
  }
  get restitution() {
    var _a, _b;
    return numOr((_b = (_a = this.__json) == null ? void 0 : _a.collider) == null ? void 0 : _b.restitution, 0.1);
  }
  get count() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.physics) == null ? void 0 : _b.bodyInfo(this.entity.id)) == null ? void 0 : _c.colliderCount) ?? 0;
  }
}
class AudioSource extends BuiltinComponent {
  __key() {
    return this.__json && typeof this.__json.id === "string" ? this.__json.id : this.entity.id;
  }
  __settings() {
    return this.__json && typeof this.__json.audio === "object" ? this.__json.audio : null;
  }
  __update(patch) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.updateSettings(this.__key(), patch);
  }
  get source() {
    var _a;
    return ((_a = this.__settings()) == null ? void 0 : _a.source) ?? "";
  }
  set source(v) {
    if (typeof v === "string") this.__update({ source: v });
  }
  get autoplay() {
    var _a;
    return ((_a = this.__settings()) == null ? void 0 : _a.autoplay) !== false;
  }
  set autoplay(v) {
    this.__update({ autoplay: v === true });
  }
  get loop() {
    var _a;
    return ((_a = this.__settings()) == null ? void 0 : _a.loop) !== false;
  }
  set loop(v) {
    this.__update({ loop: v === true });
  }
  get volume() {
    var _a;
    return numOr((_a = this.__settings()) == null ? void 0 : _a.volume, 1);
  }
  set volume(v) {
    const n = Number(v);
    if (Number.isFinite(n)) this.__update({ volume: Math.min(1, Math.max(0, n)) });
  }
  get speed() {
    var _a;
    return numOr((_a = this.__settings()) == null ? void 0 : _a.speed, 1);
  }
  set speed(v) {
    const n = Number(v);
    if (Number.isFinite(n)) this.__update({ speed: Math.min(4, Math.max(0.1, n)) });
  }
  get spatial() {
    var _a;
    return ((_a = this.__settings()) == null ? void 0 : _a.spatial) === "3d" ? "3d" : "2d";
  }
  set spatial(v) {
    this.__update({ spatial: v === "3d" ? "3d" : "2d" });
  }
  get playing() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.infoOf(this.__key())) == null ? void 0 : _c.playing) ?? false;
  }
  get paused() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.infoOf(this.__key())) == null ? void 0 : _c.paused) ?? false;
  }
  get ready() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.infoOf(this.__key())) == null ? void 0 : _c.ready) ?? false;
  }
  play() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.play(this.__key());
  }
  stop() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.stop(this.__key());
  }
  pause() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.pause(this.__key());
  }
  resume() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.resume(this.__key());
  }
  setVolume(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.audios) == null ? void 0 : _b.setVolume(this.__key(), v);
  }
}
class AnimationClip extends BuiltinComponent {
  __key() {
    return this.__json && typeof this.__json.id === "string" ? this.__json.id : this.entity.id;
  }
  __b() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.bindingOf(this.__key())) ?? null;
  }
  get clip() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.clipPath) ?? "";
  }
  set clip(rel) {
    var _a, _b;
    void ((_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.changeClip(this.__b(), rel));
  }
  get duration() {
    var _a, _b;
    return ((_b = (_a = this.__b()) == null ? void 0 : _a.clip) == null ? void 0 : _b.duration) ?? 0;
  }
  get time() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.time) ?? 0;
  }
  set time(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.setTime(this.__b(), v);
  }
  get speed() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.speed) ?? 1;
  }
  set speed(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.setSpeed(this.__b(), v);
  }
  get loop() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.loop) ?? true;
  }
  set loop(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.setLoop(this.__b(), v === true);
  }
  get autoplay() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.autoplay) ?? true;
  }
  set autoplay(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.setAutoplay(this.__b(), v === true);
  }
  get playing() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.playing) ?? false;
  }
  get paused() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.paused) ?? false;
  }
  play() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.play(this.__b());
  }
  pause() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.pause(this.__b());
  }
  resume() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.resume(this.__b());
  }
  stop() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.clipAnims) == null ? void 0 : _b.stop(this.__b());
  }
}
class SkeletalAnimation extends BuiltinComponent {
  __b() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.bindingOf(this.entity.id)) ?? null;
  }
  get clips() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.clipsOf(this.entity.id)) ?? [];
  }
  get currentClip() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.currentClip) ?? null;
  }
  get playing() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.playing) ?? false;
  }
  get clip() {
    return this.currentClip ?? "";
  }
  set clip(name) {
    this.play(name);
  }
  get speed() {
    var _a, _b, _c;
    return numOr((_c = (_b = (_a = this.__b()) == null ? void 0 : _a.nodeJson) == null ? void 0 : _b.anim) == null ? void 0 : _c.speed, 1);
  }
  set speed(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setSpeed(this.entity.id, v);
  }
  get loop() {
    var _a, _b, _c;
    const v = (_c = (_b = (_a = this.__b()) == null ? void 0 : _a.nodeJson) == null ? void 0 : _b.anim) == null ? void 0 : _c.loop;
    return LOOP_MODES.includes(v) ? v : "loop";
  }
  set loop(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setLoop(this.entity.id, v);
  }
  get autoplay() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = this.__b()) == null ? void 0 : _a.nodeJson) == null ? void 0 : _b.anim) == null ? void 0 : _c.autoplay) !== false;
  }
  set autoplay(v) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setAutoplay(this.entity.id, v === true);
  }
  get hasGraph() {
    var _a;
    return !!((_a = this.__b()) == null ? void 0 : _a.graph);
  }
  get graph() {
    var _a;
    return ((_a = this.__b()) == null ? void 0 : _a.graph) ?? null;
  }
  play(clipOrState) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.play(
      this.entity.id,
      typeof clipOrState === "string" && clipOrState ? clipOrState : void 0
    );
  }
  pause() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.pause(this.entity.id);
  }
  resume() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.resume(this.entity.id);
  }
  stop() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.stop(this.entity.id);
  }
  getParam(name) {
    const g = this.graph;
    if (!g || typeof name !== "string" || !(name in g.params)) return null;
    return g.params[name];
  }
  setParam(name, value) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setParam(this.entity.id, name, value);
  }
  ensureGraph(def) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.applyGraph(this.entity.id, def)) === true;
  }
  removeGraph() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.removeGraph(this.entity.id);
  }
  addState(opts) {
    const g = this.graph;
    if (!g || !opts || typeof opts !== "object") return false;
    const name = typeof opts.name === "string" ? opts.name.trim() : "";
    if (!name || g.states.some((s) => s.name === name)) return false;
    g.states.push({
      name,
      clip: typeof opts.clip === "string" ? opts.clip : "",
      speed: typeof opts.speed === "number" && Number.isFinite(opts.speed) && opts.speed >= 0 ? opts.speed : 1,
      loop: LOOP_MODES.includes(opts.loop) ? opts.loop : "loop"
    });
    return true;
  }
  removeState(name) {
    const g = this.graph;
    if (!g || typeof name !== "string") return false;
    const i = g.states.findIndex((s) => s.name === name);
    if (i < 0) return false;
    g.states.splice(i, 1);
    g.transitions = g.transitions.filter((t) => t.from !== name && t.to !== name);
    return true;
  }
  addTransition(opts) {
    const g = this.graph;
    if (!g || !opts || typeof opts !== "object") return false;
    const from = typeof opts.from === "string" ? opts.from : "";
    const to = typeof opts.to === "string" ? opts.to : "";
    if (!from || !to || from === to || !g.states.some((s) => s.name === from) || !g.states.some((s) => s.name === to)) {
      return false;
    }
    let id = typeof opts.id === "string" && opts.id && !g.transitions.some((t) => t.id === opts.id) ? opts.id : "";
    if (!id) {
      let n = g.transitions.length + 1;
      while (g.transitions.some((t) => t.id === `t${n}`)) n += 1;
      id = `t${n}`;
    }
    const num = (v, fb) => typeof v === "number" && Number.isFinite(v) ? v : fb;
    g.transitions.push({
      id,
      from,
      to,
      duration: Math.max(0, num(opts.duration, 0.25)),
      exitTime: Math.max(0, Math.min(1, num(opts.exitTime, 0))),
      conditions: (Array.isArray(opts.conditions) ? opts.conditions : []).filter((c) => !!c && typeof c === "object" && typeof c.param === "string" && c.param).map((c) => ({
        param: c.param,
        op: CONDITION_OPS.includes(c.op) ? c.op : "==",
        value: num(c.value, 0)
      }))
    });
    return true;
  }
  removeTransition(id) {
    const g = this.graph;
    if (!g || typeof id !== "string") return false;
    const i = g.transitions.findIndex((t) => t.id === id);
    if (i < 0) return false;
    g.transitions.splice(i, 1);
    return true;
  }
  // —— 蒙皮完全控制（对应 three 官网 animation/skinning 系列示例）——
  /** 蒙皮能力摘要（{boneCount, boneNames, morphMeshes}；未绑定模型 null） */
  get skinInfo() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.skinInfoOf(this.entity.id)) ?? null;
  }
  // —— 动作级控制（blending/morph：权重混合/淡入淡出/一次性动作/全局速度/事件）——
  /** 动作权重（确保动作在播；0 即静默层。与 play/stop 的 currentClip 语义独立） */
  setWeight(clip, w) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setWeight(this.entity.id, clip, w)) === true;
  }
  /** 动作当前有效权重（淡入淡出进行中的实时值） */
  getWeight(clip) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.getWeight(this.entity.id, clip)) ?? null;
  }
  fadeIn(clip, dur = 0.25) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.fadeIn(this.entity.id, clip, dur)) === true;
  }
  fadeOut(clip, dur = 0.25) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.fadeOut(this.entity.id, clip, dur)) === true;
  }
  /** 交叉淡化 from→to（warp=true 自动对齐相位） */
  crossFade(from, to, dur = 0.25, warp = false) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.crossFade(this.entity.id, from, to, dur, warp)) === true;
  }
  /** 单动作播放速度（与 globalSpeed 相乘生效） */
  setActionSpeed(clip, scale) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setActionSpeed(this.entity.id, clip, scale)) === true;
  }
  /** 单动作循环模式（"loop"/"once"/"pingpong"；once 定格末帧） */
  setActionLoop(clip, mode) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setActionLoop(this.entity.id, clip, mode)) === true;
  }
  /** 停止单个动作（不影响其他混合层） */
  stopAction(clip) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.stopAction(this.entity.id, clip)) === true;
  }
  /** 一次性动作：定格末帧后自动淡回基础动作（表情/挥手等） */
  playOneShot(clip, fade = 0.25) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.playOneShot(this.entity.id, clip, fade)) === true;
  }
  /** 全局播放速度（mixer 速度） */
  globalSpeed(scale) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.globalSpeed(this.entity.id, scale)) === true;
  }
  /** 订阅动作播完事件（负载 {clip}），返回注销函数 */
  onFinished(cb) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.onFinished(this.entity.id, cb)) ?? (() => {
    });
  }
  /** 订阅动作循环事件（负载 {clip}），返回注销函数 */
  onLoop(cb) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.onLoop(this.entity.id, cb)) ?? (() => {
    });
  }
  // —— 加法层（additive_blending：独立权重叠加在基础动作之上）——
  playAdditive(clip, weight = 1) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.playAdditive(this.entity.id, clip, weight)) === true;
  }
  stopAdditive(clip) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.stopAdditive(this.entity.id, clip)) === true;
  }
  // —— 骨骼级控制（本地变换读写/复位/世界坐标）——
  /** 骨骼名列表（无骨骼返回 []） */
  get bones() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.bonesOf(this.entity.id)) ?? [];
  }
  /** 骨骼层级（[{name,parent,children}]） */
  get boneHierarchy() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.boneHierarchy(this.entity.id)) ?? [];
  }
  /** 骨骼本地变换快照（rotation 为度制欧拉；未命中 null） */
  getBoneTransform(name) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.getBoneTransform(this.entity.id, name)) ?? null;
  }
  setBonePosition(name, x, y, z) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setBonePosition(this.entity.id, name, x, y, z)) === true;
  }
  setBoneRotation(name, x, y, z) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setBoneRotation(this.entity.id, name, x, y, z)) === true;
  }
  setBoneScale(name, x, y, z) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setBoneScale(this.entity.id, name, x, y, z)) === true;
  }
  resetBone(name) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.resetBone(this.entity.id, name)) === true;
  }
  /** 复位全部骨骼到绑定姿势 */
  resetPose() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.resetPose(this.entity.id)) === true;
  }
  /** 骨骼世界坐标（attach 物体/瞄准参考；未命中 null） */
  getBoneWorldPosition(name) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.getBoneWorldPosition(this.entity.id, name)) ?? null;
  }
  // —— 形态键（morph：表情/姿态权重）——
  /** 形态键清单（[{mesh, targets}]） */
  get morphs() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.morphsOf(this.entity.id)) ?? [];
  }
  setMorphWeight(mesh, target, v) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setMorphWeight(this.entity.id, mesh, target, v)) === true;
  }
  getMorphWeight(mesh, target) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.getMorphWeight(this.entity.id, mesh, target)) ?? null;
  }
  // —— IK（skinning_ik：CCD 求解，目标点/关节限位）——
  /**
   * 注册 IK 链：{ name?, effector: 骨骼名, links: [{bone, rotationMin?,
   * rotationMax?}], iteration? }（限位为度制欧拉数组）。返回 IK id，失败 null。
   */
  addIK(def) {
    var _a, _b;
    const id = (_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.addIK(this.entity.id, def);
    return typeof id === "string" ? id : null;
  }
  removeIK(id) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.removeIK(this.entity.id, id)) === true;
  }
  setIKEnabled(id, v) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setIKEnabled(this.entity.id, id, v)) === true;
  }
  setIKTargetPosition(id, x, y, z) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.setIKTargetPosition(this.entity.id, id, x, y, z)) === true;
  }
  getIKTargetPosition(id) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.getIKTargetPosition(this.entity.id, id)) ?? null;
  }
  /** IK 清单（[{id,name,effector,enabled}]） */
  get iks() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.iksOf(this.entity.id)) ?? [];
  }
  // —— 骨骼/IK 目标绑定（物体跟随骨骼；官方 ik 示例挂点语义）——
  /**
   * 把场景节点绑到骨骼/IK 目标上每帧跟随。
   * target 为 Entity 或节点 id（须在模型子树之外）；bone 传骨骼名、IK id 或
   * IK name；opts = { keepOffset?, syncRotation?, syncScale? }。
   */
  attachToBone(target, bone, opts) {
    var _a, _b, _c;
    const targetId = target && typeof target === "object" ? target.id : target;
    const entry = typeof targetId === "string" ? (((_a = state.host) == null ? void 0 : _a.registry) ?? []).find((r) => r.json && r.json.id === targetId) : null;
    if (!entry) return false;
    return ((_c = (_b = state.host) == null ? void 0 : _b.animations) == null ? void 0 : _c.attachObject(this.entity.id, entry.obj, bone, opts)) === true;
  }
  /** 解除节点绑定（target 为 Entity 或节点 id） */
  detach(target) {
    var _a, _b, _c;
    const targetId = target && typeof target === "object" ? target.id : target;
    if (typeof targetId !== "string" || !targetId) return false;
    const entry = (((_a = state.host) == null ? void 0 : _a.registry) ?? []).find((r) => r.json && r.json.id === targetId);
    if (!entry) return false;
    return ((_c = (_b = state.host) == null ? void 0 : _b.animations) == null ? void 0 : _c.detachObject(this.entity.id, entry.obj)) === true;
  }
  /** 绑定清单（[{node, bone, syncRotation, syncScale, keepOffset}]） */
  get attachments() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.animations) == null ? void 0 : _b.attachmentsOf(this.entity.id)) ?? [];
  }
}
export {
  AnimationClip,
  AudioSource,
  Collider,
  RigidBody,
  SkeletalAnimation
};
