// AUTO-GENERATED by scripts/build-runtime.mjs (source: src/runtime/**) — DO NOT EDIT.
import { state } from "./state.mjs";
import { Entity, setKindClasses } from "./entity.mjs";
class Transform extends Entity {
}
class MeshNode extends Transform {
}
class LightNode extends Transform {
}
class CameraNode extends Transform {
  /**
   * 屏幕坐标 → 世界空间射线（用于物理拾取/视线检测等）。
   * screenX/screenY 为画布内 CSS 像素（左上角原点；与 engine.input 指针坐标同一空间）。
   * 返回 { origin, direction }（origin = 相机世界位置，direction = 归一化世界方向）；
   * 相机未就绪/坐标越界返回 null。
   */
  screenToRay(screenX, screenY) {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.camera) == null ? void 0 : _b.screenToRay) == null ? void 0 : _c.call(_b, screenX, screenY)) ?? null;
  }
}
class SkyboxNode extends Transform {
}
class FogNode extends Transform {
}
class ParticleSystemNode extends Transform {
  play() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.play(this.id);
  }
  pause() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.pause(this.id);
  }
  stop() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.stop(this.id);
  }
  restart() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.restart(this.id);
  }
  clear() {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.clear(this.id);
  }
  get playing() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.infoOf(this.id)) == null ? void 0 : _c.playing) ?? false;
  }
  get paused() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.infoOf(this.id)) == null ? void 0 : _c.paused) ?? false;
  }
  get finished() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.infoOf(this.id)) == null ? void 0 : _c.finished) ?? false;
  }
  get aliveCount() {
    var _a, _b, _c;
    return ((_c = (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.infoOf(this.id)) == null ? void 0 : _c.alive) ?? 0;
  }
  get settings() {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.settingsOf(this.id)) ?? null;
  }
  setSettings(patch) {
    var _a, _b;
    (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.updateSettings(this.id, patch);
  }
}
for (const key of [
  "duration",
  "looping",
  "prewarm",
  "startDelay",
  "startLifetime",
  "startSpeed",
  "startSize",
  "startColor",
  "endColor",
  "gravityModifier",
  "emissionRate",
  "maxParticles",
  "shape",
  "shapeRadius",
  "shapeAngle",
  "simulationSpace",
  "colorOverLifetime",
  "sizeOverLifetime",
  "blending",
  "texture"
]) {
  Object.defineProperty(ParticleSystemNode.prototype, key, {
    configurable: true,
    enumerable: false,
    get() {
      var _a, _b;
      const s = (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.settingsOf(this.id);
      return s ? s[key] : void 0;
    },
    set(v) {
      var _a, _b;
      (_b = (_a = state.host) == null ? void 0 : _a.particles) == null ? void 0 : _b.updateSettings(this.id, { [key]: v });
    }
  });
}
class UICanvasNode extends Transform {
}
class UIImageNode extends Transform {
}
class UITextNode extends Transform {
}
class UIButtonNode extends Transform {
}
class UILayoutNode extends Transform {
}
class TerrainNode extends Transform {
  /** 世界高度采样（节点本地 x/z；节点仅平移时即世界坐标） */
  sampleHeight(x, z) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.terrains) == null ? void 0 : _b.sampleHeight(this.id, x, z)) ?? 0;
  }
  /** 地表平坦度（1 = 平地 → 0 = 崖壁） */
  sampleSlope(x, z) {
    var _a, _b;
    return ((_b = (_a = state.host) == null ? void 0 : _a.terrains) == null ? void 0 : _b.sampleSlope(this.id, x, z)) ?? 1;
  }
  get settings() {
    var _a, _b;
    return (_b = (_a = state.host) == null ? void 0 : _a.terrains) == null ? void 0 : _b.settingsOf(this.id);
  }
}
class FsmRunnerNode extends Transform {
}
class BtRunnerNode extends Transform {
}
const UI_ANCHOR_KEYS = ["anchorMin", "anchorMax", "pivot", "anchoredPosition", "offsetMin", "offsetMax"];
for (const [Cls, keys] of [
  [UICanvasNode, ["sortOrder", "designWidth", "designHeight", "scaleMode"]],
  [UIImageNode, ["sortOrder", "size", ...UI_ANCHOR_KEYS, "image", "color"]],
  [UITextNode, ["sortOrder", "size", ...UI_ANCHOR_KEYS, "text", "fontSize", "color", "bold", "italic", "fontFamily", "align"]],
  [UIButtonNode, ["sortOrder", "size", ...UI_ANCHOR_KEYS, "image", "color", "label", "labelColor", "fontSize", "labelBold", "interactable"]],
  [UILayoutNode, ["sortOrder", "size", ...UI_ANCHOR_KEYS, "layoutMode", "padding", "spacing", "gridColumns"]]
]) {
  for (const key of keys) {
    Object.defineProperty(Cls.prototype, key, {
      configurable: true,
      enumerable: false,
      get() {
        var _a, _b;
        const s = (_b = (_a = state.host) == null ? void 0 : _a.ui) == null ? void 0 : _b.settingsOf(this.id);
        return s ? s[key] : void 0;
      },
      set(v) {
        var _a, _b;
        (_b = (_a = state.host) == null ? void 0 : _a.ui) == null ? void 0 : _b.updateSettings(this.id, { [key]: v });
      }
    });
  }
}
const KIND_CLASSES = {
  node: Transform,
  meshNode: MeshNode,
  cameraNode: CameraNode,
  skyboxNode: SkyboxNode,
  fogNode: FogNode,
  audioNode: Transform,
  particleSystemNode: ParticleSystemNode,
  terrainNode: TerrainNode,
  fsmRunnerNode: FsmRunnerNode,
  btRunnerNode: BtRunnerNode,
  lightNode: LightNode,
  pointLightNode: LightNode,
  directionalLightNode: LightNode,
  ambientLightNode: LightNode,
  spotLightNode: LightNode,
  uiCanvasNode: UICanvasNode,
  uiImageNode: UIImageNode,
  uiTextNode: UITextNode,
  uiButtonNode: UIButtonNode,
  uiLayoutNode: UILayoutNode
};
setKindClasses(KIND_CLASSES);
Transform.__nodeKinds = null;
MeshNode.__nodeKinds = ["meshNode"];
LightNode.__nodeKinds = [
  "lightNode",
  "pointLightNode",
  "directionalLightNode",
  "ambientLightNode",
  "spotLightNode"
];
CameraNode.__nodeKinds = ["cameraNode"];
SkyboxNode.__nodeKinds = ["skyboxNode"];
FogNode.__nodeKinds = ["fogNode"];
ParticleSystemNode.__nodeKinds = ["particleSystemNode"];
TerrainNode.__nodeKinds = ["terrainNode"];
FsmRunnerNode.__nodeKinds = ["fsmRunnerNode"];
BtRunnerNode.__nodeKinds = ["btRunnerNode"];
UICanvasNode.__nodeKinds = ["uiCanvasNode"];
UIImageNode.__nodeKinds = ["uiImageNode"];
UITextNode.__nodeKinds = ["uiTextNode"];
UIButtonNode.__nodeKinds = ["uiButtonNode"];
UILayoutNode.__nodeKinds = ["uiLayoutNode"];
function isNodeRefType(v) {
  return typeof v === "function" && v !== Entity && Object.prototype.hasOwnProperty.call(v, "__nodeKinds");
}
export {
  BtRunnerNode,
  CameraNode,
  FogNode,
  FsmRunnerNode,
  KIND_CLASSES,
  LightNode,
  MeshNode,
  ParticleSystemNode,
  SkyboxNode,
  TerrainNode,
  Transform,
  UIButtonNode,
  UICanvasNode,
  UIImageNode,
  UILayoutNode,
  UITextNode,
  isNodeRefType
};
