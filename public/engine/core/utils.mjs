// AUTO-GENERATED by scripts/build-runtime.mjs (source: src/runtime/**) — DO NOT EDIT.
const D2R = Math.PI / 180;
function num(v, fb) {
  return typeof v === "number" && Number.isFinite(v) ? v : fb;
}
function u01(v, fb) {
  return Math.max(0, Math.min(1, num(v, fb)));
}
function vec(v, fb) {
  return v && typeof v === "object" ? v : fb;
}
function matColor(v, fb) {
  if (typeof v === "number" && Number.isFinite(v)) return v & 16777215;
  if (typeof v === "string") {
    const s = v.trim().replace(/^#/, "");
    if (/^[0-9a-fA-F]{6}$/.test(s)) return parseInt(s, 16) & 16777215;
  }
  return fb;
}
function mixHexColor(a, b, t) {
  const ar = a >> 16 & 255;
  const ag = a >> 8 & 255;
  const ab = a & 255;
  const br = b >> 16 & 255;
  const bg = b >> 8 & 255;
  const bb = b & 255;
  const r = Math.round(ar + (br - ar) * t);
  const g = Math.round(ag + (bg - ag) * t);
  const bl = Math.round(ab + (bb - ab) * t);
  return (r & 255) << 16 | (g & 255) << 8 | bl & 255;
}
export {
  D2R,
  matColor,
  mixHexColor,
  num,
  u01,
  vec
};
