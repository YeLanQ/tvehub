<script setup lang="ts">
// ---------------------------------------------------------------------------
// 场景 / 脚本等「无预览资产」的纯提示块（展示型，从 AssetInspector 抽出）：
// 按 kind 渲染对应的操作提示文案（分支条件仍由父组件持有，与本组件一一对应）。
// ---------------------------------------------------------------------------
defineProps<{
  /** 提示种类（scene 场景 / ts 脚本） */
  kind: "scene" | "ts";
}>();
</script>

<template>
  <div class="hint">
    {{ kind === "scene" ? "双击资产打开场景（层级/视口随之切换）。" : "双击资产或右键「打开脚本」进入脚本工作台编辑。" }}
  </div>
</template>
