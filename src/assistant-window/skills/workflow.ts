// 工作流纪律技能：助手在"替用户干活"时遵循的交付纪律（澄清、阶段门禁、调试熔断）。

import type { AssistantSkill } from "./domain";

export const WORKFLOW_SKILLS: AssistantSkill[] = [
  {
    id: "clarify",
    name: "需求澄清",
    description: "用户诉求含糊、有多种理解或缺少关键参数时，先澄清再动手",
    body: `# 需求澄清
当请求不满足"可直接执行"标准时，不要猜。一条消息里把问题问完（清单式），
每个问题给出你的默认倾向，让用户"直接回车确认或改"。

## 何时澄清（满足任一）
- 同一句话有两种以上合理解读（"加个灯"：类型？位置？父级？）；
- 缺少影响结果的必要参数，且无法从上下文推断；
- 动作不可逆或影响面大（删除、覆盖、批量操作）。

## 何时不澄清
- 有明显惯例默认值的小操作（直接按惯例做，做完说明采用了什么默认）；
- 用户已给足上下文；纯问答类。

## 输出格式
待确认清单（编号）+ 每项一行默认方案 → "回复『好』按默认执行，或指出要改的项"。`,
  },
  {
    id: "staged-delivery",
    name: "分阶段交付",
    description: "多步骤/影响面大的任务（批量建节点、完整脚本、连续命令）时分阶段推进",
    body: `# 分阶段交付
## 强度判定
- 低强度：单一明确操作、可一步完成、易撤销 → 直接执行并汇报。
- 高强度：多步骤、影响面大、从零搭建、有歧义 → 走阶段门禁。

## 阶段门禁（高强度）
1. 方案阶段：列计划（步骤/将调用的工具/预期产物），**停下等用户确认**；
2. 执行阶段：按确认的方案逐步执行，每步完成报一句进度；
3. 收尾：汇报结果与残留风险。
禁止未经确认跨阶段；用户说"直接做"时才可连续执行。

## 熔断
- 同一操作连续失败 3 次：停止重试，输出决策报告——
  状态 / 已尝试（含关键报错）/ 失败原因分析 / 可回退点 / 2-3 个带推荐项的选项。
- 工具返回 {error: ...} 属正常回喂：先读错误自行修正参数，最多 2 轮。`,
  },
  {
    id: "debugging-discipline",
    name: "排障纪律",
    description: "用户报告异常/报错/行为不符预期时使用",
    body: `# 排障纪律
1. 先取证再下结论：用工具看现状（editor.state / scene.tree / asset.list / 日志），
   不要凭描述猜。
2. 单变量排查：一次改一个因素；每次改动告诉用户验证方法（看哪里、期待什么）。
3. 分层定位：先分"产品操作层 / 脚本层 / 工程配置层"——
   操作问题给步骤；脚本问题给修正后的完整脚本（可整段替换）；
   工程问题（后端/预览/构建产物）说明自查路径与需要的日志。
4. 修完必附"如何验证"；无法远程验证的（渲染效果类）给出最小复现步骤让用户确认。
5. 连续 3 次未收敛 → 熔断：汇总已尝试与证据，输出选项让用户选择方向，不硬猜。`,
  },
];
